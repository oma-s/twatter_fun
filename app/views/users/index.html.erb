<% provide(:title, 'All users') %>
<h1>All users</h1>

<div class="digg_pagination">
    <%= will_paginate @users %>
</div>

<ul class="users">
  <%= render @users %>
</ul>

<div class="digg_pagination">
    <%= will_paginate @users %>
</div>